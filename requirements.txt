# Core dependencies
litellm>=1.40.0
python-dotenv>=1.0.0
requests>=2.28.0
chromadb>=0.4.0

# Optional dependencies (install as needed)
# pip install llamagent[cli]
rich>=13.0.0

# pip install llamagent[web]
gradio>=4.0.0

# pip install llamagent[api]
fastapi>=0.100.0
uvicorn>=0.20.0

# pip install llamagent[mcp]
mcp>=1.0.0
