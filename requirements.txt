# Core dependencies
litellm>=1.40.0
python-dotenv>=1.0.0
requests>=2.28.0
chromadb>=0.6.1  # v3.7: PR #3335 LRU thread-safety fix required for parent-child shared memory

# Optional dependencies (install as needed)
# pip install llamagent[cli]
rich>=13.0.0

# pip install llamagent[web]
gradio>=4.0.0

# pip install llamagent[api]
fastapi>=0.100.0
uvicorn>=0.20.0

# pip install llamagent[skill]
pyyaml>=6.0
python-frontmatter>=1.0  # v3.8.1 R7-#12: industry-standard frontmatter wrapper around PyYAML (Hermes/Nanobot/OpenCode all use real YAML); see fs_store/parser.py

# pip install llamagent[mcp]
mcp>=1.0.0
