name: path-ops
description: Organize project files - move, copy, delete, glob
tags: [move, copy, delete, rename, clean, organize, tidy, reorganize, remove, duplicate, glob, find, pattern]
invocation: auto-trigger
required_tool_packs:
  - path-fallback
